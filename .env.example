# EduFlow Frontend Environment Variables

# Backend API Endpoint URL
VITE_API_URL=http://localhost:4000

# Optional: Direct Gemini API key for local client-side summarizer mode (if not using backend proxy)
# VITE_GEMINI_API_KEY=""
